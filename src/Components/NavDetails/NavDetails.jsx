import "./NavDetails.css"

export const NavDetails = ({children}) => {
    return (
        <ul className="nav nav-fill details-lists-nav">
            
            {children}

        </ul>
        
        )
}
